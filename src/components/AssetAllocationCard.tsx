import React from "react";

export default function AssetAllocationCard() {
  return (
    <div
      style={{
        background: "rgba(255,255,255,0.05)",
        borderRadius: "8px",
        padding: "1.5rem",
        marginBottom: "1rem",
        height: "150px",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        opacity: 0.7,
      }}
    >
      {/* Asset Allocation Card – placeholder */}
    </div>
  );
}
